# Resources RG-1: real model-tree verification

Human-only: run `scripts/verify_real_models.py` against the real eight-model tree, compare each state and byte count with `du`, compare free space with `df`, and verify partial trees never report complete. Store command output and conclusions in `docs/superpowers/runs/2026-08-31-localmodeldesk-app/signoffs/resources-rg1.md`; add `VERDICT: PASS` on its own line only after all checks pass.
