# Resources RG-2: real resume verification

Human-only: run `scripts/resources_resume_smoke.sh`, interrupt the real small-repository download, resume it, and verify completed files are not fetched again. Store both logs, destination, byte observations, and conclusion in `docs/superpowers/runs/2026-08-31-localmodeldesk-app/signoffs/resources-rg2.md`; add `VERDICT: PASS` on its own line only after all checks pass.
