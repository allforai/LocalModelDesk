"""测试里「卡住了没有」的统一判据。

独立成模块而不是放在 conftest：裸名 `from conftest import ...` 在全量跑时会被
`tests/e2e/conftest.py` 抢走（子集跑绿、全量跑炸——本仓库今天反复踩的那种形状）。
"""

# 「卡住了没有」的判据，不是性能预算。
#
# 这类断言的形状是 `thread.join(timeout=X); assert not thread.is_alive()`，
# 想证明的是「不会永远卡住」。真正的死锁**永远不返回**，所以这个数只要足够大
# 就一定抓得住；而它一旦定得紧，抓到的就变成「此刻机器忙不忙」。
#
# 实测（2026-09-24）：test_initial_load_does_not_deadlock_real_arbiter_subscription
# 里那次调用空闲时耗时约 27ms，原阈值 0.1s 只有 4 倍余量。全量跑时上千条测试同时
# 抢 CPU，它失败过两次；人为把机器压满后能按需复现（满载 1/8 失败，空闲 0/5）。
#
# 每条这样的测试后面都跟着一个与负载无关的断言（返回值、状态），那个才是真正的
# 判据。这里只负责「它到底回来了没有」。
HANG_TIMEOUT_S = 10.0
